import { HideLongStrPipe } from './hide-long-str.pipe';

describe('HideLongStrPipe', () => {
  it('create an instance', () => {
    const pipe = new HideLongStrPipe();
    expect(pipe).toBeTruthy();
  });
});
